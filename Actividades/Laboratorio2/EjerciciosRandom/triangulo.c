#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Ingrese los 3 lados del triangulo: ");
    scanf(a,b,c);
    printf("El Triangunlo es: ");

    if (a==b) & (b==c);{
        printf("equilatero")
    } else {
        if(a!=b & b!=c & c!=a);{
            print("escaleno")
        } else {
            printf("isosceles");
        }
    }
}
